import { DimensionStyles } from '../../styles/DimensionStyles';

export interface ActivityIndicatorCircleProps {
    color: string;
}

export interface ActivityIndicatorProps extends ActivityIndicatorCircleProps, DimensionStyles {}
