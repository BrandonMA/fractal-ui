import { Layer } from '../containers/Layer';
import styled from 'styled-components';

export const Table = styled(Layer)`
    display: flex;
    flex-flow: column nowrap;
    flex-grow: 1;
    flex-shrink: 1;
    flex-basis: auto;
`;

// flex: 1 1 auto;
